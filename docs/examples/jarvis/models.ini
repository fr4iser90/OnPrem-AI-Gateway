; Sticky daily router (compose service llama → :11535).
; ONE model only — always loaded. Do not add swap candidates here.
; Experiments / smart / other quants → models-lab.ini on :11537
;   docker compose --profile lab up -d
;
; Gateway aliases (Platform → Models):
;   qwen3.6  →  Qwen3.6-35B-A3B-MTP-UD-Q4_K_XL-VL  (preferred source: chat)
;
; Per-model flags only. Do NOT put keys before the first [section].

[Qwen3.6-35B-A3B-MTP-UD-Q4_K_XL-VL]
model = /models/chat/large/Qwen3.6-35B-A3B-MTP-UD-Q4_K_XL.gguf
mmproj = /models/chat/large/Qwen3.6-mmproj-F16.gguf
ngl = 99
fa = on
jinja = 1
ctk = q8_0
ctv = q8_0
spec-type = draft-mtp
spec-draft-n-max = 2
fit = off
np = 2
c = 262144
b = 64
ub = 32
load-on-startup = true
